$:
  vscode:
    - docker:
        build: .ide/Dockerfile
      services:
        - vscode
        - docker

feat*:
  push:
    - stages:
        - name: commitlint
          image: cnbcool/commitlint
        - name: go lint all
          image: docker.cnb.cool/spencezhang/go-dev-demo/dockerfile-caches:docker.cnb.cool/spencezhang/go-dev-demo/dockerfile-caches:7f7471b720fceb4cc7f6e4e37f8a80d831add0f0
          script: |
            make lint
        - name: sync to github
          imports: https://cnb.cool/spencezhang/my-keys/-/blob/main/env.yml
          image: tencentcom/git-sync
          settings:
            target_url: https://github.com/logzh/${CNB_REPO_NAME}.git
            auth_type: https
            username: ${GIT_USERNAME}
            password: ${GIT_ACCESS_TOKEN}